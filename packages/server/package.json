{
  "name": "@vod/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/http.ts",
    "start": "bun --env-file=../../.env src/http.ts",
    "build": "tsc -b"
  },
  "dependencies": {
    "@vod/shared": "workspace:*",
    "@libsql/client": "^0.17.4",
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
