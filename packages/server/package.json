{
  "name": "@aw/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/http.ts",
    "start": "bun --env-file=../../.env src/http.ts",
    "build": "tsc -b"
  },
  "dependencies": {
    "@aw/shared": "workspace:*",
    "@libsql/client": "^0.17.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
